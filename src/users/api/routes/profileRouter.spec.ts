import { profileRouter } from './profileRouter';

describe('profileRouter', () => {
  it('should define a route', async () => {
    expect(profileRouter).toBeDefined();
  });

  //need to create test for each route (mock middleware)
});
